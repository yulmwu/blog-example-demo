{
    "name": "aws-3-tier-architecture-example",
    "scripts": {
        "start": "node app.js",
        "pm2-start": "pm2 start app.js --name aws-3-tier-app",
        "pm2-stop": "pm2 stop aws-3-tier-app",
        "pm2-restart": "pm2 restart aws-3-tier-app"
    },
    "dependencies": {
        "body-parser": "^2.2.0",
        "dotenv": "^17.2.3",
        "express": "^5.1.0",
        "mysql2": "^3.15.3"
    },
    "devDependencies": {
        "pm2": "^5.3.0"
    }
}
