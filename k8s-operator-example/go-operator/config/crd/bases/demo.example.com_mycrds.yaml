---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: mycrds.demo.example.com
spec:
  group: demo.example.com
  names:
    kind: MyCRD
    listKind: MyCRDList
    plural: mycrds
    shortNames:
    - myc
    singular: mycrd
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Desired replicas
      jsonPath: .spec.replicas
      name: Desired
      type: integer
    - description: Available replicas
      jsonPath: .status.availableReplicas
      name: Available
      type: integer
    - description: Container image
      jsonPath: .spec.image
      name: Image
      priority: 1
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: MyCRDSpec defines the desired state of MyCRD
            properties:
              image:
                description: '컨테이너 이미지 (기본값: nginx:1.25.3)'
                type: string
              replicas:
                description: 유지하고 싶은 파드 수 (Deployment replicas)
                format: int32
                type: integer
            type: object
          status:
            description: MyCRDStatus defines the observed state of MyCRD
            properties:
              availableReplicas:
                description: 현재 가용(Ready) 파드 수
                format: int32
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
